---
- name: Set Hostname for Ubuntu Machines
  hosts: all
  become: true

  tasks:
    - name: Set hostname
      hostname:
        name: "{{ ansible_host.split('.')[-1] | regex_replace('^(\\d)(\\d)$', '\\1-\\2') | regex_replace('^0', '') }}"
