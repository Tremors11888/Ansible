---
- name: Set Hostnames for Ubuntu Machines
  hosts: all
  become: yes

  tasks:
    - name: Set hostname
      hostname:
        name: "UD-{{ inventory_hostname.split('.')[2] | int | abs | regex_replace('^(\\d)(\\d)$', '\\1-\\2') | regex_replace('^0', '') }}"
